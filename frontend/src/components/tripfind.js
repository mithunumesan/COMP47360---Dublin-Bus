function GetTripSchedule(props) {
  return (<h1>Hello {props.name}!</h1>);
    
    // this.state = {
    //   line:props.line,
    //   stopname:stopname,
    //   arrTime:arrTime,
    //   stopNum:stopNum,
    // }

    // return fetch(`http://localhost:8000/api/tripschedule/?routeshortname=${line}&stopname=${stopname}&arrivaltime=${arrTime}&stopNum=${stopNum}`)
    //   .then(data => data.json())
  }

  
  